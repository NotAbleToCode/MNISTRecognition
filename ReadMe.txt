环境：python3
库：numpy， sklearn， tensorflow， matplotlib， cv2
首先在data文件夹下运行process_data.py，生成要用到的数据文件。
然后就可以运行KNN.py，SVM.py，会自动建模并估计准确率。
CNN.py，建模和预测分步骤进行，先建模，然后将建模的语句注释掉，再预测。
这里附带了已经建立好的模型，在model文件夹下。所以直接运行UI.py即可手写数字进行识别，空格识别，a键清空。
